# 版本日志 — 「今天，吃了吗？」

## V2.2.3（打卡体验优化 — 2026-06-12）

### Bug 修复
- **搜索框不展开**：`.checkin-search-area` CSS `display:none` 与 JS `element.style` 不兼容 → 模板加内联 `style="display:none"`

### 功能改进
- **打卡按时间窗预勾选**：今天打卡时，根据当前时辰只预勾已过餐次；昨天/修改模式不受影响

## V2.2.2（修复版本更新不提示 — 2026-06-11）

### Bug 修复
- **版本更新不弹窗**：本地测试后 localStorage 标记 `seen` → 线上不再提示 → 新增 `prevVersion` 检测机制，版本号变化时强制清"已看过"标记

## V2.2.1（修复水果重复+早餐渲染 — 2026-06-11）

### Bug 修复
- **水果重复**：`pickByRatio` 的 hash 偏移 503 与 1103 同模100 → 早餐加餐总是同一种水果 → 改 1103→997
- **早餐渲染**：`renderMealPlanTable` / `calculateTotals` 已从旧 `breakfast.protein` 迁移到新 `breakfast.egg` + `breakfast.dairy`
- **NaN 防御**：`fmtFood` 增加 `isNaN(grams)` 检查，避免极端情况显示 "NaNg"

### 工作流
- **双目录同步**：每次改代码后自动 sync 到 `E:\nutrition\`（Git 仓库目录），避免打开旧版本

## V2.2（食物选择全面升级——个性化+动态化 — 2026-06-11）

### 新增功能
- **🥜 坚果池扩充**：从4种扩至8种——核桃40%/杏仁20%/开心果10%/腰果10%/花生7%/松子仁5%/榛子5%/瓜子3%，按 `pickByRatio` 轮换
- **🍎 水果池大升级**：从固定7种（苹果·橙子·猕猴桃·梨·草莓·火龙果）扩至17种，按GI比例轮换（低GI 85%/中GI 10%/高GI 5%）
- **📍 水果去向7天轮换**：不再只有加餐吃水果——部分天数早餐有水果、周五午餐也有水果，总水果量保持150g/天
- **🥚 早餐蛋白质动态化**：从固定「鸡蛋2个+牛奶1杯」改为鸡蛋60%/牛奶25%/豆浆15%按比例轮换
- **🛢️ 早餐油绑定鸡蛋**：仅鸡蛋日才配油（煎蛋），牛奶/豆浆日早餐无油
- **🍇 三餐水果独立选择**：早/午/加餐各餐水果不同（不同 `hashDate` 偏移量）
- **👤 用户个性化方案**：不同用户同一天食物完全不同（`userSeed` = 用户名哈希 + 重新生成计数器）
- **🔄 重新生成真正变化**：每次点击「重新生成」食物种类随机变化（`planRegenCounter++`）

### 改动文件
- `js/meal-plan.js`：新增 `FRUIT_ITEMS`、`FRUIT_DISTRIBUTION`、`BREAKFAST_PROTEIN` 常量；`generateMealPlan` 加 `userSeed` 参数；所有选择函数接收哈希偏移
- `js/app.js`：新增 `planRegenCounter` + `getUserSeed()`；4处 `generateMealPlan` 调用传 `userSeed`
- `index.html`：版本号更新

### 技术说明
- `userSeed = stringHash(surveyState.currentUser) + planRegenCounter`
- `hashDate()` 改为 `hashDate() + userSeed + offset`，所有选择函数自动个性化
- 三水果独立选择偏移量：早餐+503 / 午餐+811 / 加餐+1103

---

## V2.1.3（修复晚餐蛋白质缺失+切换角色不刷新 — 2026-06-11）

### Bug修复
- 🐛 **修复晚餐蛋白质缺失**：`FOOD_ROTATION` 缺少 `dinnerProtein` 属性，导致 `pickProteinExcludeCategory(undefined, ...)` 报 TypeError → 方案生成崩溃。补上7天晚餐蛋白质轮换池，午晚品类自动错开
- 🔄 **修复切换家庭成员方案不更新**：`switchFamilyMember()` 静默生成新方案后未调用 `showPlanPage()` 刷新页面，导致切换后仍显示旧方案。改为 async + 自动刷新

### 影响
- 登录后可正常生成一日3餐方案（此前崩溃）
- 家庭管理中切换角色后方案立即更新

---

## V2.1.2（修复方案不显示+家庭成员不同步 — 2026-06-11）

### Bug修复
- 🔧 **修复一日3餐方案不显示**：`generateMealPlan()` 中 `FOOD_ROTATION.breakfastGrain[daySeed]` 等3行引用了已删除的数组 → TypeError → 被 try-catch 吞掉。改为 `todayGrainNames.breakfast` 等已计算好的变量
- 🔧 **修复基本信息和家庭成员不同步**：Supabase 同步时云端 `basic_info` 覆盖本地后未更新家庭成员，导致设置页两边数据不一致。改为云端同步时双向更新

### 影响
- 登录后「生成营养方案」可正常展示一日3餐表格
- 设置页基本信息和家庭管理数据保持一致

---

## V2.1.1（修复食物库bug — 2026-06-11）

### Bug修复
- 🔧 补回 `getCategories()` 函数（V2.1生成新版foods.js时丢失）
- 🔧 补回 `searchFood()` 函数（食物搜索功能恢复）
- 🔧 补回 `getFoodById()` 函数（食物详情查看功能恢复）
- 🔧 移除 `module.exports`（浏览器不存在module对象，引发ReferenceError）

### 影响
- 食物库页面恢复正常显示（此前显示"0种食物"）
- 分餐方案可正常生成（此前依赖FOOD_DATABASE的功能全部可用）

---

## V2.1（食物数据库大升级 + GI频率控制 — 2026-06-10）

### 新增功能
- **🍎 食物数据大升级**：从126种扩充至210种日常食物，全部营养数据来自《中国食物成分表》标准版第六版
- **🔢 GI频率控制**：主食按GI值低/中/高分级，按周排班自动轮换（低GI 81%/中GI 14%/高GI 5%）
- **🐟 ω-3/ω-6官方数据**：基于食物成分表脂肪酸数据，替换原手填估算值
- **🥬 不溶性膳食纤维标注**：明确数据源中纤维字段为不溶性膳食纤维

### 数据修复
- 🔧 补回7种丢失的主食（全麦面包、燕麦粥、荞麦面条、糙米饭、黑米饭、藜麦饭、紫薯）
- 🔧 补回4种丢失的蔬菜（空心菜、菜心、娃娃菜、油麦菜）
- 🔧 修复轮换池中6种不存在的食物名→替换为数据库中已有食物
- 🔧 foods.js语法通过，共210种食物全部有完整数据+GI值

## V2（方案C：核心公式迁移到云端 — 2026-06-09）

### ⚠️ 重大版本升级说明
> 架构重心从纯前端计算 → Supabase 云端计算，**核心公式不再暴露在 F12 中**

### 架构变更
- **核心公式迁移到 Supabase Edge Function**：TDEE 计算（BMI/标准体重/年龄系数/能量系数/三大营养素）全部部署到云端，前端只传参数
- **15张参考数据表**：`ref_energy_coeff`、`ref_age_factor`、`ref_bmi_threshold`、`child_energy` 等（RLS 禁止 anon 直读）
- **三层安全防线**：F12搜公式 → 云端代码挡住 | 直接curl调Edge Function → 鉴权 | anon key查表 → RLS拒绝

### 新增文件
| 文件 | 说明 |
|------|------|
| `js/supabase-api.js` | Edge Function API 调用封装（JWT鉴权+重试） |
| `sql/phase1_ref_tables.sql` | 15张参考数据表建表脚本 |
| `sql/fix_admin_account.sql` | 修复admin账号user_accounts缺失记录 |
| `backup/calculator-sensible-formulas.js` | 核心公式备份（供BOSS查阅） |
| `supabase/functions/calculate-adult/index.ts` | 成人营养计算 Edge Function |
| `supabase/functions/calculate-child/index.ts` | 儿童营养计算 Edge Function |
| `supabase/functions/calculate-meal-plan/index.ts` | 分餐方案计算 Edge Function |

### 涉及改动
- `index.html`：新增 `supabase-api.js` 引用；版本号 v1.3→v1.4
- `js/app.js`：8个函数改为 async，本地计算替换为 API 调用
- `js/survey.js`：2个函数改为 async + API调用
- `js/calculator.js`：原封不动（BOSS要备份）

### Bug修复
- 🔧 `shareBtn` 不存在报错 → 加 `?.` 可选链
- 🔧 `updateNavActive is not defined` → 加 `typeof` 保护

### 已知问题
- [待办] 儿童进阶模式（碳水50-65%滑动条）— 暂不实现，有需要再加
- 能量补偿调整的宏量营养素仍用本地计算（TDEE已知后仅做比例拆分，无敏感公式）
- admin 账号 `fhgexin@gmail.com` 需手动执行 `sql/fix_admin_account.sql` 补录 user_accounts 记录

### 回滚指南
如 V2 云端架构出现异常，可回退至 V1.3 版本：
1. `git revert HEAD~3`（回退最近3个commit）
2. 确认 Edge Function 不再被调用
3. 删除 `js/supabase-api.js` 引用

### 新增功能
- **👶 儿童公式上线**：年龄<18自动识别，跳过三档选择，调用 `calculate-child` Edge Function
- **📊 儿童计算规则**：碳水57%固定（膳食指南AMDR中位值），蛋白查RNI表，能量查EER表/体重×75
- **🛢️ 油品轮换重调**：菜籽油60%→30%，橄榄油→40%，猪油→30%（符合"鼓励橄榄油/猪油，减少高ω-6菜籽油"）
- **🛢️ 三餐独立选油**：早/午/晚各自独立选油（不同种子），一天可出2种油，不再全天一种
- **🥜 坚果轮换重调**：核桃50%↑/杏仁25%↑/花生15%↓/瓜子10%↓（优先附录3鼓励的核桃杏仁）
- **🥩 肉类轮换优化**：午餐豆腐→羊肉（肥瘦）、增加三文鱼频次×2；晚餐清蒸鱼→鸭肉
- **🐟 ω-6:ω-3 比值展示**：脂肪来源分布区新增一行，实时计算并展示ω-6:ω-3比值（>8:1标记⚠️偏高）

### 机制说明
- 所有选择保持**确定性**（hashDate()），同一天永远同一个结果
- 不是随机概率，也不是固定周期——是"日期确定性映射"
- 长期来看按比例分布（如菜籽油约30%的餐次出现）

### 待补
- 深海鱼种类有限（仅三文鱼ω-3>1000），后期BOSS提供新数据（鳟鱼、沙丁鱼等）再扩

---

## V1.2（家庭管理功能 — 2026-06-08）

### 新增功能
- **👨‍👩‍👧‍👦 家庭管理**：设置页新增家庭管理卡片，可添加/编辑/删除家庭成员
- **🔄 快速切换**：导航栏显示当前成员，点击弹出切换列表，一键切换
- **👤 独立信息**：每个成员独立姓名、关系、性别、年龄、身高、体重数据
- **💾 数据同步**：设置页修改信息自动同步到家庭成员；切换成员自动更新计算器
- **🎭 关系头像**：关系自动匹配 emoji 头像（爸爸👨妈妈👩儿子👦女儿👧爷爷👴奶奶👵）
- **🆕 首次初始化**：登录后自动将现有数据创建为第一个家庭成员，无感过渡

### 待完善
- 儿童 BMR 公式（Schofield/WHO标准），等资料到位后更新
- 家庭成员的套餐偏好、打卡记录独立存储（当前共享同一份）
- 多人数据同步到 Supabase 云端

---

## V1.1（手机端布局优化 + 安全漏洞修复 — 2026-06-08）

### 手机端布局优化
- 营养素圆圈：竖排一列 → 2x2 网格，缩减垂直高度
- 分餐方案表格：新增横向滚动容器，手机端可左右滑动查看9列
- 容器 padding 收紧，摘要数值等比缩小
- 档位选择弹窗：≤480px 从 3 列改为 1 列

### 安全漏洞修复
- **后端（Supabase RLS 策略重建）**：
  - knowledge_base：仅管理员（fhgexin@gmail.com）可写/改/删，任何人可读
  - user_accounts：普通用户只能看自己，管理员可看全部
  - reg_code_usage：仅管理员可读取，已登录用户可递增次数
- **前端（JS 代码加固）**：
  - adminLogin() 增加角色校验，非管理员无法进入后台
  - 保存密码不再存明文到 localStorage，仅存邮箱
  - 备份更新：knowledge_base 备份已同步到最新 13 条
  - 恢复：id=25「脂肪的主要来源」恢复 SQL 已就绪

### 已知问题
- 渗透测试中被删除的 id=25 数据需手动执行恢复 SQL
- 密码保存改为仅存邮箱后，"保存密码"功能不再自动填写密码字段

---

## V1（初始版本 — 2026-06-05）

### 核心功能
- 用户注册/登录（邮箱 + 验证码，Supabase Auth）
- 基本信息填写（姓名/性别/身高/体重/年龄/活动量）
- 营养计算器（BMR/TDEE/三大营养素分析 + 矿物质达标率）
- 低碳水饮食三档位选择（控制型/温和型/极低碳水）
- 每日方案自动生成（分餐轮换 + 午晚不重复品类）
- 食物营养数据库（126种食物，含矿物质&维生素）
- 每日打卡弹窗（勾选昨日食物 + 搜索自定义食物）
- 能量补偿机制（偏差拆2份等额补、单日±15%封顶、周一清零）
- 日历视图（月历网格 + 日期状态标记 + 方案vs实际详情面板）
- 用户等级系统（普通⭐90天 / VIP🌙180天 / Plus☀️365天 / 永久👑）

### 技术栈
- 前端：Vanilla JS + CSS3（无框架）
- 后端：Supabase（Auth + Database + RLS）
- 部署：GitHub Pages
- 数据同步：localStorage + Supabase 双写

### 待优化
- 小孩子计算公式（BMR/TDEE 目前只支持成人）
- 微信小程序版本开发
- 按日期/打卡记录选择性删除数据

---

## 版本号规则
- 大版本：V1, V2, V3...（重大功能革新）
- 小更新：V1.1, V1.2...（功能新增/重要bug修复）
- 补丁：V1.1.1, V1.1.2...（小bug修复）

每次更新需同步修改：
1. 本文件（添加版本记录）
2. `index.html` 中的 `.version-footer` 文字
3. `js/app.js` 中的 `APP_VERSION` 常量
4. Git commit 消息（包含版本号）
